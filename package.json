{
  "name": "personal-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./styles/output.css --minify",
    "dev": "npx @tailwindcss/cli -i ./src/input.css -o ./styles/output.css --watch",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.9",
    "tw-animate-css": "1.3.3"
  }
}
